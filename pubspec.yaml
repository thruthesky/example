name: example
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.6 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  firebase_core: ^2.15.0
  firebase_auth: ^4.7.2
  fireflutter:
    path: ../..

  go_router: ^10.0.0
  firebase_messaging: ^14.6.7

  auth_buttons:
    git:
      url: https://github.com/thruthesky/flutter_auth_buttons
      ref: cd366eecf31639198286de48af4efa9280f06c77
  firebase_dynamic_links: ^5.3.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
